kind = "web"
previewPath = "/"
title = "Personal Expense Tracker"
description = "A responsive personal finance app for recording daily spending, managing monthly budgets, organizing categories, and understanding spending patterns at a glance."
version = "1.0.0"
id = "artifacts/expense-tracker"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25762

[services.development]
run = "pnpm --filter @workspace/expense-tracker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/expense-tracker", "run", "build" ]
serve = "static"
publicDir = "artifacts/expense-tracker/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25762"
BASE_PATH = "/"
